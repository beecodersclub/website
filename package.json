{
  "name": "html-tailwind-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "watch": {
    "build": {
      "patterns": [
        "docs"
      ],
      "extensions": "html"
    }
  },
  "scripts": {
    "watch": "npm-watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "postcss input.css -o docs/output.css",
    "dev": "concurrently \"npm run build && live-server docs --port=8080\" \"npm run watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^3.1.4"
  },
  "devDependencies": {
    "concurrently": "^7.2.2",
    "esm": "^3.2.25",
    "gulp": "^4.0.2",
    "gulp-changed": "^4.0.3",
    "gulp-imagemin": "^7.1",
    "gulp-newer": "^1.4.0",
    "npm-watch": "^0.11.0"
  }
}
